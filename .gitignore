# Python
__pycache__/
*.py[cod]
*.pyo
.env
venv/
env/
.venv/

# DO NOT ignore model artifacts — they must be committed to the repo
# outputs/injury_model.joblib  ← keep this in git
# outputs/scaler.joblib        ← keep this in git
# outputs/model_meta.json      ← keep this in git

# OS
.DS_Store
Thumbs.db